{
  "_notes": "Scrapshell's root command, per docs/lore/os-lineages.md section 3 (quorum root model) -- lineage-specific, not universal like spec/probe, per docs/systems/console-commands.md ('a Scrapshell box's usr/bin should not contain elevate... a db/software/templates/ lineage base should define its own root-model command'). Only referenced from db/vfs/templates/scrapshell.json's usr/bin. Full quorum-vote state machine is out of scope for this pass -- root_claim effect reports the request as pending, doesn't model actual union-member voting yet.",
  "id": "claim",
  "kind": "software",
  "help": "claim root --union-vote -- request root via union quorum vote",
  "effect": {"type": "root_claim", "root_model": "quorum", "quorum_required": 3}
}
