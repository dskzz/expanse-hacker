# assets

Raw source assets (art, audio, fonts) before import into the Godot project under
`code/`. `raw/` holds working files (e.g. .xcf, .psd, source audio) that get
exported into engine-ready formats.
