# Filesystem Tree & Namespace Model — moved

This content has been merged into the canonical version at
[`docs/lore/os-lineages.md`](../docs/lore/os-lineages.md) (§5–6, filesystem tree and console
sketch) and [`docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md) (§2, tenant/namespace layer as an
engine responsibility) — Gary had independently built out the engine-side version of this same
idea, and it's more complete there than it was here.

Kept as a stub rather than deleted so old links/history still resolve to something.
